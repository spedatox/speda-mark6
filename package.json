{
  "name": "speda-mark6",
  "private": true,
  "workspaces": ["packages/desktop"],
  "scripts": {
    "desktop:dev": "npm run dev --workspace=packages/desktop",
    "desktop:build": "npm run build --workspace=packages/desktop",
    "desktop:typecheck": "npm run typecheck --workspace=packages/desktop",
    "desktop:web:dev": "npm run web:dev --workspace=packages/desktop",
    "desktop:web:build": "npm run web:build --workspace=packages/desktop"
  }
}
