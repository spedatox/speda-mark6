# Shell scripts and Dockerfiles MUST be LF — CRLF breaks them on the Linux server
# ("bad interpreter: /bin/bash^M"). Force LF regardless of the committer's OS.
*.sh        text eol=lf
Dockerfile  text eol=lf
Caddyfile   text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
